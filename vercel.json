{
  "version": 2,
  "buildCommand": "cd frontend/vue-frontend && npm install && npm run build",
  "outputDirectory": "frontend/vue-frontend/dist",
  "builds": [
    {
      "src": "backend/main.py",
      "use": "@vercel/python"
    }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backend/main.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
